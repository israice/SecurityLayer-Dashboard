
# TASKS

🔴 CRITICAL — Безопасность (блокирует продакшн)
- Хеширование паролей: bcrypt вместо plaintext в admins.csv
- API аутентификация агентов: токен/API-key при сборке ZIP, проверка на /update-dashboard
- Server-side сессии: HTTP-only cookies вместо sessionStorage, убрать пароль из клиента
- Ротация webhook secret: удалить из .env_EXAMPLE, сгенерировать новый, обновить GitHub
- Rate limiting: ограничение запросов на /api/login, /api/register, /api/check
- Input validation: валидация CSV на /update-dashboard, защита от CSV injection
- Сильные пароли: минимум 8 символов, цифры, спецсимволы (сейчас 3 символа)
- HTTPS only: убрать HTTP fallback в агенте, редирект HTTP→HTTPS на сервере

🟠 HIGH — Ядро функционала
- Удаление компьютера при остановке агента (stop.bat → API DELETE с ORG_ID+PC_ID → удаление строк → SSE обновление dashboard)
- Кнопки переключения таблиц в top panel: Dashboard (по умолчанию) и Users 👤 — AJAX замена таблицы без перезагрузки
- users table logic
- Inline редактирование ячеек users table (редактируемые поля в конфиге скрипта, сохранение на сервер при blur)
- Кнопка ➕ под таблицей users — добавляет пустую строку, админ заполняет inline
- Столбец 🗑️ справа в users table — иконка удаления в каждой строке, confirm popup, API DELETE полностью из CSV
- Settings в правый верхний угол, слева от неё 🔔 с красным badge (счётчик), клик → popup список, клик на сообщение → popup детали
- Логика уведомлений: событие — отключение USB, хранение в notifications.csv на сервере, прочитано при клике на сообщение
- Рефакторинг AGENT+DASHBOARD: разбить большие файлы, читабельная структура папок, настройки вверх скриптов, переменные в config.yaml
- Расширить config.yaml: интервалы, пути, API endpoints — разделить по секциям (agent/dashboard/server)
- Аудит localStorage: проверить что хранится, определить минимум, убрать лишнее, добавить нужное

🟡 MEDIUM — Инфраструктура и данные
- Hello world SQLite: отдельный тестовый проект для изучения перед миграцией CSV
- Миграция всех CSV → SQLite: полный переход, API без изменений, только бэкенд меняется
- Email минимум: подтверждение регистрации + восстановление пароля
- Аудит кода на подозрительные паттерны: найти методы, триггерящие антивирусы, заменить на безопасные альтернативы
- UI для агента: временное отключение защиты USB пользователем для замены девайса
- Гибридный режим: локальный сервер-прокси (агенты → LAN → локальный сервер → интернет → server)
- Полностью локальная версия: сервер + dashboard + SSE в LAN, без интернета, доступ только внутри сети
- Health check endpoint: /health для мониторинга и load balancer
- Audit logging: логирование login attempts, изменений конфигурации, действий агентов
- Верификация бинарников: SHA256 checksum для Python embedded при сборке ZIP

🟣 QUALITY — Качество кода
- Unit tests: pytest для auth.py, webhook.py, zip_builder.py
- Integration tests: тесты API endpoints с тестовой базой
- Type hints: типизация Python кода (mypy)
- Error monitoring: интеграция Sentry для отслеживания ошибок
- Стандартизация комментариев: выбрать один язык (EN или RU)

📚 DOCS — Документация
- API документация: OpenAPI/Swagger spec для всех endpoints
- Deployment guide: инструкция по развёртыванию (Docker, manual)
- Agent installation guide: пошаговая инструкция для конечных пользователей
- Architecture document: техническое описание системы
- Security hardening guide: рекомендации по безопасной настройке

🔵 NORMAL — UI/UX улучшения
- В правой части top panel: "CompanyName Secured" + кол-во компьютеров, зелёный стиль успеха
- Вместо "Secured" — оранжевое предупреждение "N computers not saved" если есть ПК без финализации (не все порты закрыты затычками)
- Третья кнопка Computers в top panel: та же таблица что Dashboard, но скрыты некоторые колонки + другая сортировка по умолчанию
- Cyber стиль: подобрать шрифты и цвета для org-name и Admin Dashboard, единая тема для всех других страниц
- Лого SecurityLayer в левый верхний угол header: создать иконку + текст
- Mobile таблица: горизонтальный скролл, приоритет полнота данных + brainstorm: отдельная мобильная vs адаптив
- Dashboard: клик на некоторые ячейки → popup с фиксированными вариантами → выбор → API обновление CSV
- Клик на номер версии → popup changelog из version.md, последние 5 изменений
- Live wallpaper на login: частицы/матрица/волны/градиент, легковесный WebGL
- Live wallpaper на dashboard: спокойный/минималистичный, не отвлекает от данных

🟢 LOW — Расширение платформ
- Android: hello world → SecurityLayer app (dashboard mobile) → usbSecurity агент
- iOS: hello world → SecurityLayer app (dashboard mobile) → usbSecurity агент
- Linux: мульти-дистрибутив версия usbSecurity агента
- Mac: найти эмулятор → hello world → usbSecurity агент
- Type-C: поддержка Type-C портов + on/off в настройках
- Mobile push-уведомления со звуком (Android + iOS)

⚪ FUTURE — Бизнес и маркетинг
- Кнопка Cameras в top panel: отдельная таблица cameras.csv (структура позже)
- Settings: on/off табов (Users, Cameras) в top panel + DESKTOP (разрешение Agent UI пользователям), по умолчанию только Dashboard
- выбор языка в Settings, AI-перевод на 10 топ языков с потенциалом
- Монетизация: Trial → платные тарифы (Silver=cloud, Gold=hybrid, Platinum=local), исследовать платёжные сервисы, интегрировать, создать PAYMENT_AUDIT.md
- Landing page: страницы About Team, Contacts + создать аккаунты в соцсетях
- Startup платформы: аккаунты (ProductHunt, AngelList, Crunchbase, YC + другие) + контент-стратегия для каждой → инвесторы + клиенты
- Список pentest-компаний Израиля: потенциальные партнёры → назначить встречи для бизнес-предложений
- Hardware: найти поставщика USB-затычек, заказать 50 шт для демо клиентам + тестирование
- Гранты: список программ Innovation Authority (R&D + маркетинг), выбрать с высоким шансом получения
- Pitch: универсальная презентация (slides) + устный скрипт для инвесторов/клиентов/партнёров
- Упаковка продукта: коробка с брендингом SecurityLayer (USB-затычки + инструкция + флешка с софтом)
